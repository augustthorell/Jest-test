const add = (x, y) => {
    return x + y;
}

const subtrackt = (x, y) => {
    return x - y;
}

const divide = (x, y) => {
    return x / y;
}

const multiply = (x, y) => {
    return x * y;
}

module.exports = { add, subtrackt, divide, multiply };